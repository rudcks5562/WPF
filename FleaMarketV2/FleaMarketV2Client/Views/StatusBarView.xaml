<UserControl x:Class="FleaMarketV2Client.Views.StatusBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FleaMarketV2Client.Views"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="800">
    <Grid Background="{StaticResource BackGroundBrush1}" >
        

        <StackPanel Orientation="Horizontal">
            <Button Content="MAIN OFFER" Style="{StaticResource BaseButton}" Margin="10" Padding="5" Command="{Binding MainOfferMoveCommand}" Foreground="Black" Background="{StaticResource BackGroundBrush8}"></Button>
            <Button Content="MY OFFER" Style="{StaticResource BaseButton}" Margin="10" Padding="5" Command="{Binding MyOfferMoveCommand}"></Button>
          
            
        </StackPanel>
        <Button Content="+ ADD OFFER" Style="{StaticResource BaseButton}" HorizontalAlignment="Right" Margin="10" Padding="5" Command="{Binding AddOfferRegSpawnCommand}"></Button>
    </Grid>
</UserControl>
